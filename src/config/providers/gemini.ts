import type { GeminiRealtimeProviderConfig } from "./types.js";

export const geminiRealtimeProvider: GeminiRealtimeProviderConfig = {
  provider: "gemini",
  model: "gemini-2.0-flash-live-001",
  voiceName: "Aoede",
};
