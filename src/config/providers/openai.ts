import type { OpenAiRealtimeProviderConfig } from "./types.js";

export const openAiRealtimeProvider: OpenAiRealtimeProviderConfig = {
  provider: "openai",
  model: "gpt-realtime",
  voice: "marin",
};
